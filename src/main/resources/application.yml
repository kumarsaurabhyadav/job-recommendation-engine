spring:
  application:
    name: job-recommendation-engine

  datasource:
    url: jdbc:postgresql://localhost:5432/job_match
    username: kumarsaurabhyadav
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080